# Feature and bug queue

Process requests in the order received. New requests are appended to the queue without interrupting the active item. Change that order only when the user explicitly reprioritizes or cancels work.

- Give every item a stable, increasing ID and mark it as a feature or bug.
- Record the request and a concrete completion criterion before starting it.
- Keep one item active. Finish and verify it before starting the next queued item.
- If an item cannot proceed, record the specific blocker and continue with the oldest unblocked item. Return to the blocked item when it becomes actionable.
- Keep completed items and their verification evidence below.
- After each completed fix or feature passes verification, commit it and push the new version to GitHub on the current working branch. This is an ongoing user preference.
BLOCKER (2026-09-21, re-confirmed 2026-09-25): pushing to github.com fails with HTTP 403 from git-receive-pack — the corporate Zscaler proxy (server: Zscaler/6.2) blocks git push POSTs while allowing fetch/ls-remote and the REST API. SSH ports are also blocked (22 refused, 443 reset). Local commits accumulate on master until the network allows pushes; item commits are verified and safe locally. Remote layout: origin has NO master — refs are main (gfx-integration merge, diverged from local master at merge-base c9b5cc1), flash-muzzle and gfx-integration. When pushes become possible, use `git push origin master:refs/heads/master` (a new remote branch — never force onto main); reconciling local master with the diverged origin/main is a deliberate follow-up, not an automatic merge.
## Active

None - the next finished queued item is promoted here.



## Queued

**042 — Feature: Command layer and per-side perspective (multiplayer prerequisite).**

Request: Prepare the sim for multiplayer (plan: `docs/superpowers/plans/multiplayer.md` §7 P1–P2). All UI-to-sim mutations become serializable commands applied at tick boundaries, and the sim stops assuming a single human `playerSide`.

Completion: `Battle.submit(command)` queues orders, deploy moves, truce, flee, pause and speed, applying them at the start of a sim tick in canonical order. No UI or render module calls `issueOrder`, `deployTeam`, `flee` or `addMessage` on sim state directly. `controllers: Record<Side, 'human' | 'ai'>` replaces `playerSide`/`aiBothSides` in the sim. Messages carry a side tag, and each side's debrief is graded from its own perspective. A test battle with two human sides runs without AI orders, and the harness still passes with both sides on AI. Single-player plays unchanged; full test suite and `npm run build` pass.

**043 — Feature: Determinism fixes, state hash and replays (multiplayer prerequisite).**

Request: Make lockstep determinism verifiable (plan §2 D1–D7, §7 P3–P5, where it is called 012).

Completion:
- The crater height-field sync happens in the sim (`leaveCrater`), and render no longer writes `BattleState` (the `unitRender.ts` muzzle snap).
- `Rng` exposes its state, and `hashState` covers RNG state, time, soldiers, vehicles, VLs and scores.
- Battles record a command log that can be saved and replayed.
- A replay run in a fresh Node process reproduces the identical per-tick hash chain.
- `tools/determinism.html` gives identical hash chains in Chrome, Firefox, Safari and Node for 5 maps × 2 seeds. If it does not, the sim moves to a deterministic `src/shared/dmath.ts`, with a test banning `Math` transcendental functions under `src/sim`.

**044 — Feature: Online multiplayer (umbrella).**

Request: "plan multiplayer to the backlog. We should ponder 2 possibilities. 1) local games connecting to each other over internet, and 2) the game runs on a server and each player connects to the server and plays it." Both are evaluated in `docs/superpowers/plans/multiplayer.md`. The recommendation is deterministic lockstep over a WebSocket relay (option 1), with the authoritative server (option 2) kept as a later path for ranked play.

Blocked on: 042, 043 and the user decisions in plan §8 (1v1 or co-op, AI availability, lobby/ranked, relay hosting, map-hack tolerance, speed and pause policy, Safari support, scope).

Completion: plan milestones M1–M5 are met.
- Two browsers on different networks choose their own forces and deploy.
- They play a full battle through the deployed relay with matching state hashes.
- Pause and speed follow the agreed policy.
- A disconnect offers AI takeover or forfeit.
- A reloaded tab rejoins by fast-forwarding the command log.
- A desync produces a downloadable log that reproduces offline.

The next request receives ID 045.

## Completed

| ID | Type | Request | Result |
| --- | --- | --- | --- |
| 041 | Bug | Campaign-mode force-pool purchases are silently discarded | ForcePicker pool clicks in campaign mode now mint persistent campaign teams: addTeam with the year-legal pool def (same Rng scheme as op seeding), atomic deduction of campaign.requisition, and the bought team rides BOTH parallel lists (rosterIds + campaignUids) as its own uid so the operation's Next selects it into the battle — previously the raw defId landed in a uid list and the purchase vanished. Un-fielding (second roster-row click) splices both lists so retire/refit/upgrade never index a misaligned campaignUids; the bought team stays in the kampfgruppe (no refund — it was paid at acquisition). The picker's points display reads campaignState.requisition in campaign mode (battle mode keeps points−spent); the fielding fit-check moved off the spend balance — selectForces gained an allowance param (default campaign.requisition, existing tests unchanged) and the operation's Next passes op.requisition, so acquisitions/refits no longer break the next launch; advanceOperation now refreshes campaign.requisition after op.index += 1 (briefing N gets op N's allowance, not N−1's). Battle mode keeps raw-defId purchases. Verified: test/requisition.test.ts +6 (mint/deduct/parallel lists, points and slots exhaustion, un-field persistence, selectForces acceptance under the allowance, battle-mode unchanged); regression surface (requisition/operation/rosterScreen/campaignStructure) 29 pass; full suite 1006 pass / 2 skip / 0 fail, tsc clean. Browser E2E: new operation → COA → briefing → requisition bought a ger_rifle_41 (t9): requisition 220→190, remaining() 190, lists parallel; trimmed the command team (row splice from both lists, no refund); Next → deploy → Begin — 9 German teams fielded in the battle with the bought rifle present (two ger_rifle_41 squads, 10-man rosters). |
| 037 | Feature | Manual back-cover input card: every key the original reserved, actually wired | Z/X/C/V/B/N/M arm the seven order types (selection required, matching the original's select-then-command flow); right-click during aiming cancels the order line without placing the dot; ESC quits the current battle without saving — battle AND deploy both show a "QUIT CURRENT BATTLE WITHOUT SAVING?" confirm, YES returns to the main menu leaving the operation intact and writing nothing to the chronicle, NO resumes, ESC/N cancel, Y confirm; SPACEBAR now shows command radii instead of pausing (F3/PAUSE keep the pause, PAUSED banner added); F3-F8 preventDefault against their browser defaults; CTRL+K dead overlay, CTRL+S hard mute that survives pause cycles, CTRL+T tree hiding (a battle-view readability aid: canopies skip, units under them plainly visible) — note: Chrome/Firefox reserve CTRL+T before the page, the binding works only under a shell that captures keys first; CTRL+M/CTRL+V intentionally unbound: the game has no music track or replay/video system and message-only stubs would be fake compliance. inputCard.test.ts (7) + full suite 983 pass, tsc clean; every key verified live in the browser with synthetic input. |
| 036 | Feature | Pre-battle briefing screen (roadmap G4) + round-7 UI consistency pass | BriefingScreen sits between COA and Operation: grand-campaign situation text, battle brief with objectives list, terrain thumbnail with numbered objective markers; COA next label reads "Briefing →". ESC backs out of briefing→COA and operation→menu, and every other menu screen (battle setup, requisition, boot camp, history, roster) backs out consistently; Options ESC saves and returns; debrief ESC advances like the continue button. Minimap plain click jumps the camera like the original, hold continues as drag-pan. Team status bars re-measured against the reference capture (~11-13 px, no outline). Aim-range feedback carries the aim line's LOS class. Verified in the live g…
| 034 | UX | On-map marking layer per the original | Every friendly unit (teams and tanks) carries the vivid green leader bar, clamped to the view edge when the team is partly off-screen and hidden at zoom-out per the manual's "bars only at normal zoom"; order markers are glossy navy balls with specular highlight and drop shadow — no connecting line, no distance labels; the strip right of Combat Messages is empty maroon; the soldier monitor is docked at the right edge with its bottom at the view's edge. Vision re-review vs refs12: all four criteria pass. |
| 040 | Bug | Roster screen unreachable — strip Soldiers button hardcoded disabled | BottomStrip gains a soldiersEnabled flag; the operation screen (briefing mode), the debrief and the COA screens enable it and route Soldiers clicks to RosterScreen with a `back` reference so Continue/ESC return to the calling screen (operation picker, debrief or COA). Roster: real names seeded per soldier for new campaigns and repaired on load; table columns re-spaced to fit the panel; team-grouped rows materialized as header entries in the scroll list so the wheel-scroll clamp (entries minus viewport) always reaches the last soldier. test/rosterScreen.test.ts (8): strip flag routing, rows() header/soldier materialization with empty teams skipped, scroll clamp boundary, ESC/strip return, entry-point wiring for all three screens. |
| 035 | UX | Deploy group select per the manual | Manual group-select is fully live in the deploy screen: Shift-click toggles teams in and out of the group (including clicks that land between a crew's men — the forgiving pick now resolves the tightest containing formation, not the deepest bounding circle, so a small mortar crew beats the rifle columns that overlap it); plain click on empty ground still arms the marquee, a real drag box-selects (Shift-marquee adds to the selection), a click that never lands on a soldier resolves the forgiving pick and empty ground deselects; clicking and dragging one group member drags every selected team with its exact relative offset preserved (grab tile under the cursor, per-team drop tiles, invalid drops rejected with the red flash) and redeploy falls back to the default order; group orders keep offsets for Move/Fast/Sneak and centre on Fire/Smoke. Begin branch restored (a clobbered action dispatch had silently dropped it). Verified live end-to-end: select, toggle, marquee, empty marquee, group drag delta (39/44 both moved exactly -1,-1 with ambush defaults), group order offsets, marker press keeps selection, Begin reaches BattleScreen. Full suite 976 pass / 0 fail, tsc clean. |
| 019 | Feature | Missing historical maps — Kremlin and the Berlin final battle, plus the rest of the original set | Four new DSL maps — Stalingrad Red Barricades factory district (ruined factory slabs, rubble, Volga edge), Korsun pocket breakout (snow, villages, frozen river with bridge), Vistula bridgehead at Magnuszew (floodplain, dike/trench line, wide river), Kremlin quarter finale (dense city blocks, Moskva, the Kremlin itself) — plus the Berlin final-assault map; campaign rewired to the 16 historical operations ending in the two-battle Götterdämmerung finale (city, then Reichstag quarter). |
| 001 | Feature | Slow crew panic bail-out | Crews climb out, drop down, remain prone in shock, then crawl toward cover. |
| 002 | Feature | Ctrl+number group assignment and number selection UI | Numbered groups support keyboard assignment/recall and HUD buttons with team counts and active selection. |
| 038 | Bug | Armoured engagements fizzle — vehicles never re-acquire and the AI drives them blind | The commander's eye (2.2 m) and the main gun's muzzle (1.5 m) now sit at different heights: a tank in defilade behind a crest sees over it while its gun line stays masked, so it creeps forward until the muzzle clears and only then fires (combat.ts gunLine + creepForGun; los.ts VEHICLE_GUN_M; the round itself launches from the muzzle). A vehicle that loses sight keeps its lay on the last-known belief for the 30 s window (vehicleAreaTarget belief-target path). The blind-loop overwatch post now requires a gun line to the watch point (ai.ts chooseOverwatch vehicleMover). Seeded moscow_1941 seed 9 AI-vs-AI: 22 main-gun rounds in 300 s (was 1). Tests: test/armourExchanges.test.ts (6). |
| 003 | Feature | Guesstimate fire through obscured areas with vegetation interactions | Vehicles and MGs scatter fire around ordered areas or recent beliefs; rounds cut vegetation, damage/remove woody cover, and can deflect with visible effects. |
| 004 | Bug | Tanks slide sideways and backwards when seeking cover | Tanks follow reachable routes with hull-aligned movement, turn toward side cover, use aligned reverse when appropriate, settle in cover, and resume their prior route after the threat clears. |
| 005 | Feature | Release Shift to finish movement waypoint placement at the last placed point | Move, Move Fast, and Sneak now finish on Shift release without another click. Earlier points remain ordered waypoints; the last becomes the destination. Empty chains stay in targeting mode, Escape cancels, and window blur does not submit a route. |
| 006 | Feature/bug | Soldiers move and animate too fast; aiming takes practically no time | Infantry walk at 1.1 m/s and run at 2.4 m/s, with crouch/crawl caps of 0.7/0.3 m/s and slower emergency, carrying, and crew movement. Animation steps follow distance without phase jumps; idle and posture changes are slower. Soldiers turn, raise, and settle before firing, with acquisition affected by range, handling, training, fatigue, and suppression. Moving troops halt to aim, then resume; target changes, relocation, and interruptions require reacquisition. Existing vehicle and crew weapon firing timing is preserved. |
| 007 | Feature | SMG aimed, hip, and prone fire with torso rotation, recoil, sweeping bullets, and panic-related magazine dumps | MP40 and PPSh rounds fire individually at timed cyclic intervals. Aim and shot bearings drive torso poses, recoil, flashes, and traced bullets. Close engagements can use less accurate hip fire; prone fire is steadier. Stress and inexperience increase full-magazine sweeps, which stop on interruptions. New sprite atlases cover both sides, seasons, and resolutions. |
| 008 | Feature | Hasty fire during assault, heavy defensive pressure, or panic | Close assaults and defensive pressure allow faster, less accurate small-arms fire. Physical rays scatter; suppressed shooters hesitate between shots. Full panic occasionally allows a brief erratic shot/burst while preserving hesitation/flight, visibility, weapon cycles, reloads, and severe-state interruption. |
| 027 | Bug | House occupants visible as if from the top floor | House interiors were a goldfish bowl: 'floor' tiles carried only 0.6 concealment, so a ray through a window showed the room's occupants almost plainly at any range — reading as top-floor visibility. floor concealment 0.6 -> 0.85: men deep in a room are invisible even through a window (4 interior tiles accumulate >1), a man right at the glass stays visible. Windows/adjacency rules unchanged. |
| 028 | Feature | Tanks didn't destroy AT guns they rolled over | stepOverrun now smashes any enemy crew-served gun whose pivot passes under the hull (CrewWeaponState.destroyed): cleared mission/workers, no re-manning, no orders, hasChamberedRound refuses it, stepCrewWeapons skips it. Friendly guns are never crushed. Renderer draws the wreck as the packed sprite tipped askew with scorch and splinters. |
| 024 | Bug | Options realism toggles were dead flags | All four wired: BattleConfig carries alwaysSeeEnemy / neverActOnInitiative / alwaysFullEnemyInfo / alwaysObeyOrders (copied from game.settings at every battle start — setup, operation, boot); neverActOnInitiative gates subordinateInitiative; alwaysObeyOrders skips the motivation/fear refusal roll (daze/radio-range gates still apply); alwaysSeeEnemy short-circuits the render visibility gate (cheat sight, sim spotting untouched); alwaysFullEnemyInfo draws enemy team bars like friendly ones. Base options (volume, labels, LOS lines, speed, vision/depth overlays, info bar) audited — all had live consumers already. |
| 022 | UX | Tanks move only along their tracks | driveReversing no longer slides straight at its destination: it backs along the hull's rear axis and the driver steers the course by turning the hull; the lined-up forward branch drives along the hull axis (not a straight line to the waypoint) so an off-axis waypoint is approached by an arc. Turn rates already follow physics: tracked vehicles pivot in place (neutral steer) at the historical turn-in-place figure, wheeled vehicles only steer at speed (rate = speed / turnRadius). |
| 026 | Feature | Ricochets | Non-penetrating hits were resolved but invisible: state.sparks was pruned but never drawn and armorClank/penHit events had no audio. drawSparks (render/effects.ts) draws the full Spark set (armor = bright ricochet fan + hot core, pen = red-white flash, dust/wood/stone/brick puffs, body fleck, backblast ring); sfx wires armorClank -> ricochet ping, penHit -> muffled ricochet; the shooter's team gets a 'Ricochet! The round bounces off the <name>'s armour.' message on every clean bounce (outcome stays 'bounced' — immobilised/gear outcomes keep their own messages). |
| 015 | Feature | Blasted gun crews go unconscious/dead | blastCrew() (vehicleDamage.ts, exported) runs after rollDeathOutcome in the kill path: 'explosion' kills or incapacitates nearly everyone (max 1 walks away whole), 'turretBlown' maims the turret crew (>70% hurt) while the driver usually crawls out, fire/hatchBlown wound ~half and kill almost none outright, a quiet stop hurts nobody; hurt men stay aboard (the climb-out skips them). Determinism test also gained a bun-runner timeout (5 s default flaked). |
| 016 | Bug | Vehicle MGs have a field of fire — no sideways fire | The coax MG engages only within the turret's field of fire (±30° of turretFacing on turreted vehicles; the casemate's gunArcRad along the hull axis for SPGs); the bow MG keeps its ±15° hull arc. pickCoaxTarget gates BOTH the attack-unit branch and the nearest-infantry fallback with inCoaxMgArc (combat.ts). Tests: test/coaxArc.test.ts (helper geometry, turreted vs casemate arcs) and test/coaxFireArc.test.ts (no coax tracers 90° off the turret bearing; engagement when the turret bears — victim there killed by the main gun, asserted via tracer kind not health). |
| 017 | Bug | Dragging the move endpoint drops the previous waypoint | Reissue on marker drag now preserves the chain: viewKeys.reissueOrderOnMarkerDrag returns `{...order, target: drop}` (waypoints intact) for an endpoint drag and `{...order, waypoints[i] = drop}` for a waypoint-dot drag; battle.ts marker-drag release issues it instead of the old type-only re-issue that cleared waypoints. A stale drag (order type changed, or waypoint index past the surviving chain — a consumed leg) is dropped. Tests: test/markerDragWaypoints.test.ts (endpoint drag keeps both waypoints; waypoint drag rewrites only its point; fire endpoint retarget; stale drags null). |
| 018 | Feature/bug | Tanks default to MGs against infantry; main gun is not wasted | combat.ts gains mainGunAtInfantry(state, vehicle, soldier) — returns 'group' (MG_GROUP_MIN=3 spotted men of the team within MG_GROUP_RADIUS_TILES=6 of each other), 'mgOut' (coax AND bow out of ammo/absent), or 'escalated' (MG engagement on that team for MG_ESCALATE_S=20 s and still below MG_SUPPRESS_LEVEL=60 pinned), else null. Strong cover = omniCoverAt >= 0.8 (trench, bunker interior BUNKER_COVER on a map.bunkerId tile, stone-building floor at 0.5) OR tileAt === 'buildingStone' (its 0.85 is directional/linear only, omni sees 0.5) OR behindGunShield — an AT-gun crewman behind his plate is a casemate, not HE fodder. mgOut also counts DESTROYED mounts, not just empty ammo: coaxUsable/bowMgUsable (systemState !== 'destroyed') gate alongside the ammo checks — a tank with a knocked-out coax but a live bow still keeps the gun silent. pickVehicleTarget gates both the firing-team branch (lone firing rifleman no longer draws HE) and the nearest-infantry fallback; AP never routes to infantry (aimPoint.ts already routes soft targets to HE). noteMgBurst tracks the engagement per vehicle (Vehicle.mgTargetKey/mgSince) after every coax/bow burst — the bow counts as the same engagement. Tests: test/mainGunDoctrine.test.ts (lone man no-target, group trigger + spread boundary, strong-cover override incl. a real stamped bunker interior, a stone-building tile, and a shield-shadowed PaK crewman (gun turned to face the tank, gunner within the 1.2 m plate half-width), mgOut via ammo AND via destroyed mounts, escalation clock incl. pinned stop, pickVehicleTarget honours the doctrine, 40 s end-to-end: 0 main-gun shots, coax bursts fired); test/ammoTypes.test.ts updated to a 3-man group (lone rifleman would never load HE now). |
| 029 | UX | Hovering a tank shows nothing about what it is doing | The hover ring now draws the vehicle's actual sim path (dashed green, distinct from the blue issued-order line) with the route end marked, and a thin red line from the hull to its current attack target — order lines only cover issued orders, not the pending hull path or the AI's own aim. The ring is suppressed on the already-selected team. |
| 030 | UX (roadmap G23) | Deploy-phase facing does not follow the defend arc | applyOrder pivots the vehicle hull+turret and crew-served gun to the Defend/Ambush order bearing at issue time (orders.ts) so the deploy-screen sprite matches the arc; DeployScreen's default armour Defend now targets the enemy deploy-zone centre instead of the team's own feet. Tests: test/deployOrders.test.ts (tank turns hull+turret immediately at issue time with the sim not run, survives into the battle, no garbage snap on a same-position target; crew gun pivots to the arc direction). |
| 031 | Feature (G31) | Ambushing teams could be seen from far away | Ambush deploys camouflage nets: while the team holds still (ambushing, not moving, not firing recently) its visual signature drops to a quarter and beyond 60 m it cannot be spotted at all (spotting.ts CAMO_NET_VISIBILITY/CAMO_NET_MAX_SPOT_M, applied in updateSpotting); the flag clears when a movement order is issued and the net is effectively shed the moment the men move or open fire. Tests: test/camoNet.test.ts (netted ambusher spotted far less often across 40 seeds, near-field signature still reduced, firing sheds the net). |
| 014 | Feature | Burning vehicles illuminate their surroundings | FIRELIGHT_RANGE_M (80 m): updateSpotting collects burning wrecks per side; a soldier target within the glow and with LOS to a spotter is always spotted (prone, still, silent); enemy vehicles within the glow of a burning wreck get the 10 m always-spot floor, a burning wreck itself the 30 m floor; damaged-engine 1.5x floor preserved. |
| 032 | Bug | Panicked tank backs across the whole map and off it | A fleeing vehicle's flight is now bounded: FLEE_MAX_DISTANCE_TILES (30) from where THIS flight began (fleeAnchor set at cycle start; cleared when the threat clears) — after the budget it holds ground and fights (or the crew bails) instead of starting a new reverse cycle. Both drive paths refuse to step off the map: a reverse that would leave the battlefield stops the hull at the border (it stands facing the threat until the cooldown forces re-engagement), and forward stepping stops at the border too. Tests: test/fleeMapEdge.test.ts (panic reverse at the north and south edges stays on the map; flight budget ends the cycle at the anchor distance). |
| 013 | Feature | Knocked-out vehicles remain ammunition hazards | resolveVehicleHit no longer ignores wrecks: only a round that PENETRATES the wreck's armour can find the rack (a bounce sparks off; HE excluded) — then the probability scales with the loaded fraction of the ready rack (turret hits most exposed, hull-side 0.7, hull 0.4) — and ignition sets burning + rackFire so the existing cook-off path (pops → detonation or fuel blast) takes over; AI target picking still skips wrecks (no wasted shots), ordered/player fire works. |
| 033 | Feature (G7) | Bunkers were invisible walls | Authored bunkers (map DSL `bunkers`) now render as concrete pillboxes: one 'bunker' decor item per bunker (firing slot facing east/west by hash) drawn by decorSprites with base shadow, sunlit edge, dark embrasure and sandbag lip, drawn from the sim-merged map.decor so authored + generated items both show; the pillbox is a solid obstacle to tracked vehicles (path.ts blocks bunker tiles for tracks); interior cover unchanged (BUNKER_COVER). Tests: test/bunker.test.ts (decor count/centre for village_1942's German pillbox, interior omni cover >= 0.8, open ground nearby uncovered). |
| 012 | Feature | Historical casemate gun barrel traverse | Verified + locked with tests: StuG III G 12°, Marder III 21°, SU-76 16° arcs already modelled (gunArcRad); the gun lays within the arc at the handwheel rate without hull movement, the hull turns only when the target leaves the arc (wantsHullTurn), fine-lay clamp never points the gun outside the arc. |
| 011 | Feature | Varied vehicle death and panic bail-out | Vehicle deaths draw from a weighted outcome set — explosion, engine fire, hatch blowout with fire spewing from the open hatches, turret thrown, gun drooping, or a quiet stop when an empty hull is murdered; crews climbing out of a burning tank are mostly burnt (wounded/scarred, rare lucky escape); panic bail-out of a working tank: inexperienced crews readily, veterans only under extreme whole-crew panic. |
| 010 | Bug/feature | Explosion protection and posture | Blast injury, suppression, knockback, stun and recovery all scale with physical shelter and posture (blastExposure.ts): trenches/craters/rubble/stonewalls/buildings shield, walls only protect between the man and the burst, interior detonations bypass tile shelter; exposed standing men are injured ~4× more than prone and thrown up to 15 m, sheltered men barely moved; vehicle ammunition/fuel explosions resolve one wave against each man's original posture (no double knockdown); direct-fire explosive rounds affect bystanders through the same splash. |
| 009 | Feature | Separately carried machine-gun mounts | A capable assistant carries the physical mount, moves more slowly and keeps his hands occupied. Carrier incapacity drops the mount for physical recovery. MG34/MG42 tripod guns can become light MGs; the Maxim waits for a carrier. Deployed guns stay mounted after assistant casualties. Equipment and carrier poses reflect the actual load, including transport boarding and riding. |
| 020 | Feature | Missing historical units — King Tiger and the rest of the original roster | VEHICLE_DEFS grew by 19 (Tiger II, Panther D/A, Pz IV G, StuG IV, Hetzer, Flammpanzer III, Kettenkrad, Kübelwagen, SdKfz 251/1 rocket halftrack, IS-1/3, SU-100/122/152, T-28, OT-34, Sherman 76, BM-13 Katyusha) and TEAM_DEFS by 23, with historical armour/weapons/crew/speed and ready-rack sizes; WEAPONS gained kwk43_88, d5t_85, zis5_76, stuk39_75, kwk40_143, m120_152, d10_100, n30_122, k28_76, nebel41, bm13 and the flammewerfer; teamsForYear gates the 1944/1945 heavies; sprites in the house style. Tests: catalog (refs resolve), rockets (BM-13 salvo, Nebelwerfer lay/pacing/reload). |

Verified together on 2026-09-19: 77 test files passed; 653 tests passed and 2 pre-existing tests skipped. `npm run build` passed, including `tsc --noEmit`. Control-group HUD and vegetation effects were also checked in the browser.

Item 005 verified on 2026-09-19: nine new tests exercise keyboard/mouse events through the battle screen, including quick Shift releases. Full suite: 78 files passed, 662 tests passed, 2 pre-existing skips. `npm run build` passed, including `tsc --noEmit`.

Item 006 verified on 2026-09-19: movement/posture limits, crew hauling without teleporting, crowd separation, physical animation cadence, acquisition and interruption behavior, move/aim/fire/resume, and full battle-loop pause/resume covered by regression tests. Existing arrival tests now wait for the slower movement. Browser preview checked with the real infantry simulation and unit renderer. Full suite: 80 files passed, 705 tests passed, 2 pre-existing skips. `npm run build` passed, including `tsc --noEmit`; `git diff --check` passed. Logs: `/private/tmp/cc3-infantry-final-tests.log`, `/private/tmp/cc3-infantry-build.log`.

Item 007 verified on 2026-09-19: 84 test files passed, 745 tests passed, 2 pre-existing skips. `npm run build` passed, including `tsc --noEmit`; `git diff --check` passed. Regression coverage includes individual ammunition consumption, burst interruption, physical swept rays, infantry/vehicle/terrain interception, suppression, posture and torso animation, muzzle alignment, single-round sound, and sprite assets. The live SMG preview was checked with no browser errors. Logs: `/private/tmp/cc3-smg-final-tests.log`, `/private/tmp/cc3-smg-build.log`.

Item 008 verified on 2026-09-19: 85 test files passed, 754 tests passed, 2 pre-existing skips. `npm run build` (including `tsc --noEmit`) and `git diff --check` passed. New tests cover rifle, pistol, SMG and LMG acquisition/accuracy, defensive pressure, distance, mechanical cycling, reloading, visibility, probabilistic panic fire, and broken/cowering states. Existing suppression-rate regression remains passing. Logs: `/private/tmp/cc3-hasty-final-tests.log`, `/private/tmp/cc3-hasty-final-build.log`.

Item 009 verified on 2026-09-19: 87 test files passed, 784 tests passed, 2 pre-existing skips. `npm run build` (including `tsc --noEmit`) and `git diff --check` passed. New regressions cover carrier selection/incapacity, loaded hands, portable fallback and ammunition conservation, mounted firing after casualties, heavy-gun immobility and normal packing, physical mount/gun recovery, blocked setup approaches, remounting, reload/movement resumption, transport boarding order and cargo, and renderer equipment/pose placement. The live mount preview confirmed separate carried loads, left-behind tripods, mounted firing after assistant loss and the Maxim's Need carrier status. Logs: `/private/tmp/cc3-mount-final-tests.log`, `/private/tmp/cc3-mount-final-build.log`.

Item 010 verified on 2026-09-19: 48/48 blast-related tests pass (blast.test.ts, blastProtection.test.ts 16 new, shieldAndCollision.test.ts, vehicleExplosion.test.ts), full suite 739 tests — vitest green across repeat runs; one intermittent bun-runner-only flake in the cook-off window test (passes in isolation, unrelated to item 010's code paths), tsc clean.

Item 011 verified on 2026-09-19: test/vehicleDeath.test.ts (5 new tests) covers the weighted outcome coverage, empty-hull majority stop, hatch/fire render state, turret state wiring, and casemate exclusion; vehicleDeath+vehicleExplosion+vehicleDamage 37/37; full suite 744 tests / 0 fail; tsc clean. Renderer: drawHatchFire (per-hatch flame columns + hull glow, deterministic flicker) wired to vehicleDamageView hatchesBlown/engineFire.

Item 012 verified on 2026-09-19: test/casemateArc.test.ts (5 tests) — historical arc values, full-circle turret arcs, inside-arc no-hull-turn vs outside-arc hull-turn, handwheel-rate timing relationship, and the fine-lay clamp; tsc clean, suite green.

Item 013 verified on 2026-09-19: test/wreckAmmo.test.ts (5) — full rack ignites reliably, empty rack never, fuller rack > nearly-empty rack, burning wreck inert, bounced rounds never ignite; tsc clean.

Item 014 verified on 2026-09-19: test/firelight.test.ts (4) — glow radius const, prone enemy near fire spotted, same enemy without fire not, beyond glow not; full suite 758/0 fail, tsc clean.

Item 015 verified on 2026-09-19: test/blastCrew.test.ts (4 direct blastCrew cases); mutation-checked in test/vehicleSpotFloor.test.ts (3) which also locks the item-014 firelight reveal and the damaged-engine 1.5x spot floor with revert-mutation verification (both mutations caught, restore verified). Full suite 765/0 fail, tsc clean.

Items 022+026 verified on 2026-09-19: test/trackKinematics.test.ts (3) — reverse moves along the rear axis only (sideways dest => no lateral slide), aligned reverse reaches the destination, a 27° off-axis waypoint is approached by an arc (<14° of the old hull axis per step); spark/clank wiring checked by tsc + suite. Full suite green, tsc clean. Cook-off and determinism tests got explicit bun-runner timeouts (5 s default flaked at 600 seeded sims).

Item 024 verified on 2026-09-19: test/realismOptions.test.ts (3) — order accepted out of command range with alwaysObeyOrders, zero subordinateInitiative events under neverActOnInitiative over a 60 s run, cheat-sight config contract; full suite green, tsc clean.

Item 027 verified on 2026-09-19: test/houseVisibility.test.ts (2) — deep interior blocked through a window, window-adjacent occupant still seen; full suite 0 fail, tsc clean.

Item 028 verified on 2026-09-19: test/overrunGun.test.ts (2) — enemy gun under the hull destroyed and inert, friendly gun untouched; related suites 38 pass, full suite 0 fail, tsc clean.
Item 016 verified on 2026-09-21: test/coaxArc.test.ts (helper geometry, turreted vs casemate arcs) and test/coaxFireArc.test.ts (no coax engagement 90° off the turret bearing; engagement when the turret bears) pass. Full suite: 106 files, 809 tests passed, 2 pre-existing skips. `npx tsc --noEmit` clean.
Item 017 verified on 2026-09-21: test/markerDragWaypoints.test.ts (endpoint drag keeps both waypoints; waypoint-dot drag rewrites only its point; fire endpoint retargets; stale drags return null). Full suite: 106 files, 809 tests passed, 2 pre-existing skips. `npx tsc --noEmit` clean.
Item 018 verified on 2026-09-21: test/mainGunDoctrine.test.ts (lone man draws no main gun, group trigger with spread boundary, strong-cover override incl. bunker interior/stone tile/shield-shadowed PaK crewman, mgOut via ammo and destroyed mounts, escalation clock, pickVehicleTarget honouring the doctrine, 40 s end-to-end: zero main-gun shots with coax bursts) and test/ammoTypes.test.ts (3-man group loads HE). Full suite: 106 files, 809 tests passed, 2 pre-existing skips. `npx tsc --noEmit` clean.
Item 029 verified on 2026-09-21: full suite 106 files / 809 tests green, `npx tsc --noEmit` clean; browser confirmation of the path/target overlay follows in the UI sweep.
Item 030 (G23) verified on 2026-09-21: test/deployOrders.test.ts new describe blocks (tank hull+turret snap to the order bearing at issue time, battle keeps the lay, same-position target leaves facing; gun pivots within its slew) pass. Full suite: 106 files, 809 tests, 2 pre-existing skips. `npx tsc --noEmit` clean.
Item 031 (G31) verified on 2026-09-21: test/camoNet.test.ts (spot-rate comparison across 40 seeds, near-field reduction, firing sheds) passes; full suite 106 files / 809 tests green; `npx tsc --noEmit` clean.
Item 032 verified on 2026-09-21: test/fleeMapEdge.test.ts (north/south edge stops, in-bounds assertion) passes; full suite 106 files / 809 tests green; `npx tsc --noEmit` clean.
Item 033 (G7) verified on 2026-09-21: test/bunker.test.ts (one decor item per bunker at its centre, interior cover >= 0.8, nearby open ground < 0.5) passes; full suite 106 files / 809 tests green; `npx tsc --noEmit` clean.
Item 020 verified on 2026-09-21: test/catalog.test.ts (every vehicleDefId/weaponId referenced by a team exists across SPECS, HEIGHT_M, DIMENSIONS; every vehicle's weapon ids resolve; the requested catalog pieces incl. ger_nebel present) and test/rockets.test.ts (BM-13 walks a full 8-rail salvo with no LOS, arc restraint says "launch rails cannot reach that bearing" once per 12 s, dry launcher silent; towed 15cm Nebelwerfer lays ~7-10 s, paces one tube per 0.5 s, reloads the next six from the 36-round reserve). Full suite: 107 files, 813 tests passed, 2 pre-existing skips; `npx tsc --noEmit` clean. Rocket min ranges scaled to the clone's map scale (nebel 180 m, BM-13 240 m — the historical 700/1000 m minima are unplayable on ≤440 m maps).

Item 034 verified on 2026-09-24: live-render regression in the browser — border_1941 battle with a mixed german force (3 teams + 3 tanks + a lone team); every friendly unit carries the green leader bar (pixel scan: 6 infantry clusters + 3 vehicle bars), partially off-screen teams keep their bar clamped to the view edge (by was previously culled), bars absent at 2:1 zoom-out; navy waypoint balls render with no connecting line and no distance labels; strip right of Combat Messages is empty maroon; soldier monitor docked at the right edge with ammo counts (MP40 32 rds, MG34 60, Kar98k 5), roles, green status words and the yellow watched-row bracket. Vision-model re-review vs refs12 composite: all four criteria pass.

Full-suite regression surfaced 16 failures: 15 from bun:test 1.4.2 lacking vitest's vi.stubGlobal/unstubAllGlobals (test/waypointInput.test.ts, test/inputEditing.test.ts rewritten to set/clear globalThis.window directly) and 1 real sim bug — near-miss suppression leaked through walls because losTrace never terrain-checked the END tile, so a ray from a shooter to a man standing just past a wall face (inside the wall's own tile) read "clear". Fixed: losTrace now blocks on a solid end tile (windows keep their from/into pass-through, concealment of the target's own tile unchanged), and both suppression sites (areaImpact and the soldier-target miss branch) gate on the shooter's line of fire. test/smgFire.test.ts regression: a MP40 burst at a stonewall column leaves the man 0.2 tiles beyond it healthy AND unsuppressed. Full suite green, tsc clean.
**019 verified on 2026-09-24**: test/historicalMaps.test.ts (all 10 map ids load; the four new maps carry the completion bar — VL count, deploy-zone sanity, season gating), test/campaignStructure.test.ts (16 operations; the finale carries the city battle then the Reichstag quarter), test/data.test.ts, test/probe_elev.test.ts (kremlin Spree street crossing elevation). Visual regression in the browser at 1:1: Kremlin renders river + street grid + building blocks, Stalingrad factory slabs + rubble yard, Vistula floodplain + trench line, Korsun snow + bridge; deploy zones drawn per item 025; VL flags render. Full suite green (970 pass), tsc clean.

**038 verified on 2026-09-25**: test/armourExchanges.test.ts (6) — eye/muzzle split LOS over a 2.0 m crest, hull-down creep sets before the shot and the tank advances then fires once the muzzle clears, an unclimbable crest never fires and the creep lapses, the belief lay holds the last-known aim inside the 30 s window and drops after, pickVehicleTarget falls back to the belief point, and the seeded moscow_1941 seed 9 AI-vs-AI census counts ≥10 main-gun rounds on both sides in 300 s. Full suite green (989 pass), tsc clean.

**039 verified on 2026-09-25**: test/moraleRecovery.test.ts (3) — a routed team with only stale beliefs recovers out of broken as the belief staleness decays the 0.4 threat floor (morale climbs once incoming fire stops); a re-shared 'reported' belief carries the original sighting's age through the share chain (no perpetual stress floor); an intact AT gun keeps tank stress up while a destroyed one does not. Full suite: 130 files, 992 passed, 2 pre-existing skips, 0 fail. `npx tsc --noEmit` clean.

**040 verified on 2026-09-25**: test/rosterScreen.test.ts (8) — the menu-strip Soldiers flag routes clicks only when enabled; rows() materializes one header entry per non-empty team before its soldiers and skips empty teams; the wheel-scroll clamp counts header rows so the final soldiers always reach the viewport (down-notches clamp at entries-minus-viewport, up-notches return to 0); ESC and the strip Continue return to the calling screen when provided, else the operation screen; the operation briefing-mode strip, the COA strip and the debrief strip all enable Soldiers when a campaign exists. Browser verification (event pump + screenshots): menu → operation → COA → briefing → picker → deploy → battle → flee → debrief → Soldiers → roster (named soldiers render, wheel scroll to bottom and back to top) → ESC back to debrief; COA → Soldiers → roster → ESC back to COA. Full suite: 131 files, 1000 passed, 2 pre-existing skips, 0 fail. `npx tsc --noEmit` clean.

**041 verified on 2026-09-25**: test/requisition.test.ts (6 new) — a pool purchase mints a persistent campaign team (addTeam with the year-legal def), deducts campaign.requisition atomically and fields it through both parallel lists (rosterIds + campaignUids) with the display reading the live balance; purchases are refused when points or slots are exhausted; removing a roster row un-fields the team from both lists but keeps it in the kampfgruppe with no refund; the bought team passes selectForces under the operation allowance (op.requisition); battle mode keeps raw-defId purchases. Regression surface (requisition/operation/rosterScreen/campaignStructure) 29 pass; full suite 1006 pass / 2 pre-existing skips / 0 fail across 131 files. `npx tsc --noEmit` clean. Browser E2E: bought ger_rifle_41 (t9) at the operation requisition — requisition 220→190, remaining() 190, lists parallel; trimmed the seeded command team; Next → deploy → Begin fielded 9 German teams with the bought rifle (two ger_rifle_41 squads, 10-man rosters).
